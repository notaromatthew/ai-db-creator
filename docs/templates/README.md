# Research Templates — DRAFT / UNAPPROVED

Machine-fillable companions to `docs/19`–`docs/28`. Copy into a versioned research package; do not change approval fields to `approved` without an authorised human decision. CSV files contain headers only. JSON files are examples and must receive real hashes during freeze.
